# Camera Configuration (PRO VERSION)
# Optimized for Raspberry Pi 4 / 5

# Default resolution (Full HD)
resolution: "1920x1080"

# Shutter speed ("Auto" or specific value like "1/100s")
shutter_speed: "Auto"

# Autofocus (Enabled by default for capable cameras)
autofocus_on_startup: true

# Camera Tuning File (Optional, leave empty for default)
tuning_file: ""
